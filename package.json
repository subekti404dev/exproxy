{
  "name": "exproxy",
  "scripts": {
    "start": "bun index.js",
    "build": "ncc build index.js -o dist && bun run build:bin",
    "build:bin": "bun build ./dist/index.js --compile --outfile exproxy"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "aes256cbc-enc": "1.0.6",
    "axios": "^1.5.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.0"
  }
}